# 依存ライブラリのセキュリティアップデート（idna 3.15）

このファイルは、仕様駆動開発の**7つの工程**のうち原則決定工程を除く6工程を一つのドキュメントにまとめた記録です。
工程ごとにファイルを分けると発散するため、全工程を1ファイルで管理します。
原則決定工程はCONSTITUTION.mdを確認してください。

---

## ② 企画・要件定義工程

### 目的

GitHub Dependabot からのアラートを機に、依存ライブラリを互換性のある範囲で最新版にアップデートする。

### 背景

- GitHub Dependabotより以下のアラートが報告されている（open のもの）

  | # | パッケージ | 深刻度 | 脆弱性概要 | 修正バージョン |
  |---|-----------|--------|-----------|--------------|
  | #50 | idna | medium | `idna.encode()` への特殊入力で CVE-2024-3651 の修正をバイパスし DoS が可能（CVE-2026-45409） | 3.15 |

- `idna` はコード内で直接 import しておらず、`anyio` および `httpx` の間接依存として引き込まれている
- `pyproject.toml` には直接宣言されていないため、`pyproject.toml` の変更は不要

### 要件

- `idna` を 3.15 以上に更新する
- 全パッケージを互換性のある最新バージョンへ一括更新する

---

## ③ 設計計画工程

### 方針

- `pyproject.toml` への変更は不要（`idna` は直接依存ではなく、全パッケージ更新で自動解決される）
- アラートの指摘の有無に関わらず、全パッケージを互換性のある最新バージョンへ一括更新する
  - バックエンド：`uv lock --upgrade && uv sync`
  - フロントエンド：`npm update`

### ファイル別変更計画

| ファイル | 変更種別 | 変更内容の概要 |
|---------|---------|--------------|
| `spec-ai-writer/uv.lock` | 修正 | `uv lock --upgrade && uv sync` で全 Python パッケージを最新化 |
| `spec-ai-writer/frontend/package-lock.json` | 修正 | `npm update` で全フロントエンドパッケージを最新化（package.json のバージョン制約に変更なし） |

---

## ④ タスク分割工程

各タスクは依存関係の順序で実施する。

### タスク一覧

- T1: `uv lock --upgrade && uv sync` で全 Python パッケージを最新化
- T2: `npm update` で全フロントエンドパッケージを最新化

---

## ⑤ 実装工程

- **実装日**: 2026-05-22
- **担当**: 高橋 篤剛

計画との乖離なし。

- idna は 3.14 → 3.16（修正バージョン 3.15 を超えて最新版が適用された）

---

## ⑥ 検証・受入工程

### 試験項目表

| 項番 | 操作 | 期待値 | 結果 |
|-----|------|--------|------|
| N-01 | `uv sync` を実行する | 正常終了する | OK |
| N-02 | `uv run pytest` を実行する | 全件パスする | OK |
| N-03 | `npm install` を実行する | 正常終了する | OK |
| N-04 | `npm run test` を実行する | 全件パスする | OK |

モバイル表示：なし（パッケージバージョン更新のみのため）

---

## ⑦ 移行・運用工程

### PR 作成

- **ブランチ名**: `takahashi/20260522-update-dependencies`
- **PR タイトル**: `fix: 依存ライブラリのセキュリティアップデート（idna 3.15）`

### 運用への影響

- `idna` は `anyio` / `httpx` の間接依存であり、アプリケーションコードへの影響なし（修正はバリデーション強化のみ）

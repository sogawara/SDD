# タスク分割工程

このファイルは、仕様駆動開発の**タスク分割工程**で作成されるタスク分割書です。

参照: [要件定義](./02-planning-requirement.md) | [設計計画](./03-design-planning.md)

## マイルストーン

{% if milestones is string %}
{{ milestones }}
{% else %}
| マイルストーン | 目標日 | 達成条件 |
|---------------|--------|----------|
{% for milestone in milestones %}
| {{ milestone.name if milestone is mapping else milestone }} | {{ milestone.date if milestone is mapping and milestone.date is defined else "（未定）" }} | {{ milestone.criteria if milestone is mapping and milestone.criteria is defined else "（未定）" }} |
{% endfor %}
{% endif %}

## タスク一覧

{% if tasks is string %}
{{ tasks }}
{% else %}
| ID | タスク名 | 担当 | 優先度 | 工数見積 | 状態 |
|----|----------|------|--------|----------|------|
{% for task in tasks %}
| {{ loop.index }} | {{ task.name if task is mapping else task }} | {{ task.assignee if task is mapping and task.assignee is defined else "（未定）" }} | {{ task.priority if task is mapping and task.priority is defined else "中" }} | {{ task.estimate if task is mapping and task.estimate is defined else "TBD" }} | {{ task.status if task is mapping and task.status is defined else "未着手" }} |
{% endfor %}
{% endif %}

## タスクの依存関係

{% if dependencies is string %}
{{ dependencies }}
{% elif dependencies is mapping %}
{% for task, depends_on in dependencies.items() %}
- **{{ task }}** は以下に依存:
{% for dep in depends_on %}
  - {{ dep }}
{% endfor %}
{% endfor %}
{% else %}
依存関係は随時更新されます。
{% endif %}

## 優先度の定義

- **高**: 最優先で取り組むべきタスク
- **中**: 通常の優先度のタスク
- **低**: 余裕があれば取り組むタスク

## 進捗管理

### ガントチャート

（プロジェクト管理ツールで管理）

### バーンダウンチャート

（スプリントごとに更新）

---

**変更履歴**

| 日付 | 変更内容 | 変更者 |
|------|----------|--------|
| {{ generation_date }} | 初版作成 | Spec AIライター |

---

*このドキュメントは Spec AIライター によって自動生成されました*
*プロジェクト名: {{ project_name }}*
